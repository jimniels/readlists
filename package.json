{
  "name": "readlists",
  "version": "0.3.0",
  "description": "",
  "type": "module",
  "main": "",
  "scripts": {
    "build": "rm -rf build && cp -R www build && npm run build:transpile && node scripts/build.js",
    "build:transpile": "babel build/js/components --out-dir build/js/components",
    "start": "http-server www",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.10.4",
    "@babel/core": "^7.10.4",
    "babel-plugin-htm": "^3.0.0",
    "http-server": "^0.12.3"
  },
  "dependencies": {}
}
